'use strict';

/**
 * prayer-director-message router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::prayer-director-message.prayer-director-message');
