'use strict';

/**
 * prayer-director-message controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::prayer-director-message.prayer-director-message');
