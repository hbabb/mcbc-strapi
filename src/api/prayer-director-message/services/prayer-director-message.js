'use strict';

/**
 * prayer-director-message service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::prayer-director-message.prayer-director-message');
